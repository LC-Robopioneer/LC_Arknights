# 年度技术学习总结

## 第一部分：在校期间已完成的学习内容

### 一、校赛项目实践学习
通过学习江科大教学视频，掌握了GPIO、TIM、UART、I2C等外设在标准库下的基本使用方法以及PID控制算法的基本原理。首先基于标准库完成了超声波跟随小车项目，并实现了HC06蓝牙通信控制功能。随后尝试使用HAL库完成了灰度循迹车的代码编写工作，但因时间限制未进行实际参数调试。

**总结**：初步建立了嵌入式项目开发的基本认知，但在实际工程应用方面仍需进一步积累经验。

### 二、M3508电机驱动与闭环控制系统

#### 1. CAN通信技术
通过观看中科大教学视频，系统学习了CAN通信的基本原理，并完成了相关代码的编写实现。同时掌握了识别CAN通信反向线的方法。

#### 2. PID闭环参数调试
初始尝试对减速前位置进行闭环控制，效果不甚理想。随后重新编写部分代码，改为对减速后位置进行闭环控制，取得了良好的控制效果。通过实践进一步掌握了PID参数的调试方法，积累了参数调整的实际经验。同时学习了前馈PID控制算法的应用（本项目未实际使用）。

#### 3. Ozone调试工具应用
通过引入Ozone调试软件，有效解决了Keil调试环境下数据可视化程度低、刷新率不足的问题，同时避免了额外配置UART进行串口绘图的复杂度。

#### 4. 代码规范性与模块化
相较于校赛项目，本次项目的代码模块化程度和规范性均有显著提升。底层User_drv库基于祖传代码移植而来，可读性得到改善，但仍需进一步完善注释规范。

### 三、底盘控制系统调试尝试
通过阅读底盘控制源代码，学习了斜坡函数的作用原理与应用方法。通过观看中科大教学视频，系统掌握了全向轮、麦克纳姆轮、舵轮等常见底盘的正逆解算方法。

**近期规划**：继续学习中科大视频，重点学习底盘欧拉角解算方法及功率控制策略。

### 四、夹爪升降机构调试参与
参与了夹爪升降机构的参数调试工作。

**主要控制逻辑**：夹爪首先定速度回到机械限位位置，由于PID调节作用而增加扭矩，当扭矩增大到一个值后认为到达限位处。随后以恒定速度运动至接近目标位置，最后采用定角度控制精确到达指定位置。

### 五、布瑞特编码器调试实验
在STM32F103C8T6平台上完成了布瑞特编码器的代码实现。

实现了完整的类封装，根据数据手册提供了所有必要的函数接口。
- **成功方面**：配置了定时回传位置功能，效果良好。在Keil调试环境中参数回传快速准确，验证了重启后位置信息包含原始角度数据，而非断电重置。
- **待改进方面**：尝试配置定时回传角速度功能，效果不甚理想。由于角速度测量需要时间间隔Δt，导致响应存在明显延迟。自主软件计算方案可能更为合适。

**总结**：定时回传模式仅支持位置或角速度单一参数回传。建议将位置设置为定时回传模式，角速度参数则建议通过软件计算实现（如实际需要）。

## 第二部分：假期期间计划完成的学习内容

### 一、CRC校验算法学习与应用
系统学习了CRC校验算法原理，编写了完整的CRC校验库。通过类封装实现了大部分常用CRC校验多项式，并完成了相应的计算函数编写。

### 二、平衡小车项目开发尝试

#### 1. 遥控器部分（已完成）
将江科大OLED显示屏（标准库软件I2C）代码移植至HAL库硬件I2C平台。将成熟代码移植到STM32F103C8T6平台，通过ADC读取摇杆数据，OLED显示相关参数，HC05蓝牙模块作为主机进行通信，采用定时回调抢占方式实现。

**技术改进**：借鉴DR16遥控器代码的紧凑内存结构体设计，通过指针类型转换（转为uint8_t*）优化数据传输结构，避免了额外分隔符和字符串传输的开销。引入了CRC校验算法（查表法），并增加了独立看门狗功能以防止系统死锁。

#### 2. 小车部分（进行中）
计划采用FreeRTOS实时操作系统完成开发。
- **当前进展**：正在重写底层驱动库，尝试使用信号量、队列、互斥锁等机制确保资源（如缓冲区）在多任务抢占环境下不会丢失。
- **后续计划**：
  1. 学习卡尔曼滤波算法用于MPU6050数据处理，重点解决俯仰角解算问题。
  2. 深入研究FreeRTOS任务管理机制。
  目标在三周内完成小车主体功能的开发。

### 三、中科大教学视频持续学习
目前剩余4-5个视频未学习（仅计算底盘篇之前及进阶篇内容），将持续推进学习进度。